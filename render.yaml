services:
  - type: web
    name: tathvyn
    runtime: python
    buildCommand: pip install uv && uv sync --frozen --no-dev
    startCommand: uv run tathvyn server --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: TATHVYN_ENVIRONMENT
        value: production
